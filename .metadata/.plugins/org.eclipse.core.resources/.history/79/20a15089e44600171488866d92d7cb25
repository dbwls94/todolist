(function (window) {
	'use strict';

	// Your starting point. Enjoy the ride!
	
	//현재 적용된 필터 -> all
	//전체 리스트 호출
	
})(window);

var todoArray = new Array();

//insert todo
$('.new-todo').keyup(function (e){
	if(e.keyCode == 13){ 
		var todoCont = $(this).val();
		
		if(todoCont == ''){
			alert('add your todo!');
			return;
		}
		$(this).val('');
	
		$.ajax({
			  type: "POST",
			  url: "/api/todo",
			  data: todoCont,
			  success: function(data){
				  console.log(data);
				  //add to '.todo-list'
				  if($('#all').hasClass('selected') || $('#active').hasClass('selected')){
					  $('.todo-list').prepend('<li><div class="view" id="' + data.id + '"><input class="toggle" type="checkbox"><label>' + data.todo + '</label><button class="destroy"></button></div></li>');
				  }
				  
				  console.log(data.length);
				  //'' item left++
				  $('.todo-count').attr('strong', data.length);
			  }
		});
		
		//'.new-todo' 자리 다시 빈 문자열로 갱신
		//$('.new-todo').val() = '';
	}
	
})